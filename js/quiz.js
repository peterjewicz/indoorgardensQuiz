var quiz = (function(){

    var currentState = 0;
    //needed to hold the items that were removed at each step
    //so we can go backwards
    var filteredItems = [];



    /**
    * @private
    * bind click events needed for the quiz
    */
    function _bindEvents(){

    }

    /**
    * @public
    * handles changing the view of the quiz
    */
    function changeState(){

    }

    /**
    * @private
    * handles filtering the array on state change;
    */
    function _filterArray(){

    }


    /**
    * @public
    * Gathers and renders the final result
    */
    function returnResult(){

    }

    /**
    * @public
    * resets the quiz i.e reload page for now
    */
    function resetQuiz(){

    }


})()
